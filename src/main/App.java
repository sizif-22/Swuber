package main;
import java.awt.*;

import javax.swing.*;

import gui.Frame;
import gui.homerightpanels.BookedShuttleBuses;

public class App {
	public static void main(String[] args) {
		new Frame();
		// JFrame frame = new JFrame();
        // frame.setTitle("Swuber");
        // frame.getContentPane().setBackground(new Color(55,55,55));

        // frame.setSize(1200, 800);
        
        // frame.setDefaultCloseOperation(frame.EXIT_ON_CLOSE);
        // frame.setResizable(false);
        // frame.setLayout(new BorderLayout());
        // frame.setLocationRelativeTo(null);
        // frame.setVisible(true);
        
		// JPanel left = new JPanel();
		// left.setBounds(0, 0, 300, 800);
        // left.setLayout(null); 
        // left.setBackground(new Color(33, 33, 33));

		// frame.add(new BookedShuttleBuses());
		// frame.add(left);
	}
}